[project]
name = "sqli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.13.2",
    "tqdm>=4.67.1",
]

[dependency-groups]
dev = [
    "ipython>=9.8.0",
]
